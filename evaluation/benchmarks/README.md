# Benchmarks

Define reusable evaluation suites here: task intent, inputs, scoring method, controlled variables, and interpretation guidance.

Benchmarks may reference shared datasets and prompts. Agents must never depend on benchmark files at runtime.
