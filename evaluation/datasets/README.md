# Datasets

Store small reusable evaluation inputs here. For large, generated, restricted, or frequently changing datasets, store reproducible retrieval/generation instructions instead of committing the data blindly.

Datasets are laboratory inputs, never agent runtime dependencies.
